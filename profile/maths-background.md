---
learning_mode: on          # off disables all probing and adaptation
owner: template            # replace on your private branch (e.g. maddy-learning)
---

# Maths background

Blank template. `main` ships this empty; your real profile lives on a private branch.

## Mastery ladder
Each concept is graded on a ladder that separates *mechanics* from *intuition/transfer* —
so "worked through the definition" is distinct from "can relate it to my own work":

| Level | Meaning | How Phase B should pitch |
|---|---|---|
| `unseen` | never encountered | teach from scratch |
| `seen` | aware of it, not worked through | build up from basics |
| `followed` | worked through the definition/derivation; can reproduce it (mechanics, not intuition) | **skip re-deriving; build intuition + worked examples + connect to their work** |
| `applied` | can use it independently in new problems | recall only; clarify edge cases |
| `intuitive` | intuitive, transferable grasp; can relate to own work and explain why it matters | use freely as a building block |

Concepts carry an optional **`target`** (the level you're aiming for) so Phase B knows the
goal. Movement is dated; provenance is `self-reported` or `demonstrated`. The `ingest`
skill probes only *new* concepts per material; Phase B updates this conversationally.

Row format: **concept** | level | target | confidence | provenance | last-updated

## Concepts
<!-- e.g. | eigen-decomposition | applied | intuitive | high | demonstrated | 2026-01-10 | -->

## Prerequisite chains observed
<!-- e.g. concept X builds on Y (seen) + Z (followed); suggested order to unlock X -->

## Notation conventions I use
<!-- e.g. bold lowercase = vector, bold uppercase = matrix, hat = estimate -->
